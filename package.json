{
  "name": "dsa-tracker-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/server",
    "packages/shared"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces",
    "seed": "npm run seed --workspace=apps/server"
  },
  "devDependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
